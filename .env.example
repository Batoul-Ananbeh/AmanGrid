# Local development only. Copy to .env and replace the password before Docker Compose use.
POSTGRES_DB=amangrid
POSTGRES_USER=amangrid
POSTGRES_PASSWORD=replace-with-a-local-password

# Browser-visible API origin used by the Vite development server and production build.
VITE_API_BASE_URL=http://localhost:8000
